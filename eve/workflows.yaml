workflows:
  process-document:
    trigger:
      system:
        event: doc.ingest
    steps:
      - agent:
          name: doc_processor
          prompt: "Process the ingested document using your doc-processor skill."
